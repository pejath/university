<div class="container mt-5 d-flex justify-content-center">
  <div class="card">
    <div class="card-body">
      <h1 class="text-center mb-4">Редактирование кафедры</h1>

      <%= render 'form', department: @department %>

      <div class="text-center mt-4">
        <%= link_to 'Show', @department, class: "btn btn-secondary me-2" %>
        <%= link_to 'Back', departments_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
