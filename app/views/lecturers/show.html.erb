<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @lecturer.name %>
</p>

<p>
  <strong>Department:</strong>
  <%= @lecturer.department.name %>
</p>

<p>
  <strong>Academic Degree:</strong>
  <%= @lecturer.academic_degree %>
</p>


<table>
  <thead>
  <tr>
    <th>Subjects</th>
    <th colspan="3"></th>
  </tr>
  </thead>
  <tbody>
  <% @lecturer.subjects.map do |subject| %>
    <tr>
      <td><%= subject.name %></td>

    </tr>
  <% end %>
  </tbody>
</table>


<%= link_to 'Edit subjects', new_lecturers_subject_path%> |
<%= link_to 'Edit', edit_lecturer_path(@lecturer) %> |
<%= link_to 'Back', lecturers_path %>